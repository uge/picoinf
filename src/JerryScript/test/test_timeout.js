print(`starting`)

let ms = 25;

Delay(ms);
print(`after delay ${ms} ms`)

Delay(ms)
print(`after delay ${ms} ms`)

Delay(ms)
print(`after delay ${ms} ms`)

Delay(ms)
print(`after delay ${ms} ms`)

Delay(ms)
print(`after delay ${ms} ms`)

for (let i = 0; i < 10000000; ++i)
{

}